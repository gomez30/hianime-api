{
  "version": 2,
  "buildCommand": "npm install && npx tsc",
  "outputDirectory": ".",
  "env": {
    "ENABLE_LOGGING": "false"
  },
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.js"
    }
  ]
}
